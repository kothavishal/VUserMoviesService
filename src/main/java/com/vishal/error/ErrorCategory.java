package com.vishal.error;

/**
 * Enum of the error codes that the API can raise.
 */
public enum ErrorCategory {

    AUTHENTICATION,
    EXTERNAL_SERVICES,
    INTERNAL_ERRORS,
    INVALID_ID,
    INVALID_INPUT,
    USERS
}
